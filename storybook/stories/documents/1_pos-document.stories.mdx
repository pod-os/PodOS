import { html } from "lit-html";

import { Canvas, Meta, Story } from "@storybook/addon-docs/blocks";

<Meta
  title="Document"
  args={{ uri: "https://angelo.veltens.org/slides/2019/solid-einfuehrung.pdf" }}
  parameters={{
    actions: {
      handles: ['pod-os:link'],
    },
  }}
/>

## pos-document

Renders the contents of a document, like a PDF or an HTML page.

<Canvas withSource="open">
  <Story name="pos-document">
    {({ uri }) => html`
      <pos-document src=${uri}>
      </pos-document>
    `}
  </Story>
</Canvas>

import { html } from "lit-html";

import { Canvas, Meta, Story } from "@storybook/addon-docs/blocks";

<Meta
  title="LDP Container"
  args={{ ldpContainerUri: "https://pod-os.solidcommunity.net/" }}
  parameters={{
    actions: {
      handles: ['pod-os:link'],
    },
  }}
/>

## pos-container-contents

Shows a list of all things that are contained in an LDP container resource. The
surrounding `<pos-resource>` should therefore refer to an LDP container.

<Canvas withSource="open">
  <Story name="pos-container-contents">
    {({ ldpContainerUri }) => html`
      <pos-resource uri=${ldpContainerUri}>
        <pos-container-contents />
      </pos-resource>
    `}
  </Story>
</Canvas>

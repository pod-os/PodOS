import {
  html
} from "lit-html";

import {
  Canvas,
  Meta,
  Story
} from '@storybook/addon-docs/blocks';

<Meta
  title="Navigation"
  args={{uri: "https://solidproject.solidcommunity.net/profile/card#me"}}
  parameters={{
    actions: {
      handles: ['pod-os:link'],
    },
  }}
/>


## pos-navigation-bar

An input field to navigate to a specific resource.

<Canvas
  withSource="open">
  <Story
    name="pos-navigation"
  >
    {({uri}) => html`
      <pos-navigation uri=${uri} />
    `}
  </Story>
</Canvas>

## Full text search

For search to work, you need to be signed in and have a [private label index](https://github.com/pod-os/PodOS/blob/main/docs/features/full-text-search.md).

> ATTENTION: To be able to log in from here, you have to go to the `Canvas` tab and click
> the `Open canvas in new tab` button in the upper right corner of the page

<Canvas
  withSource="open">
  <Story
    name="pos-navigation with search"
  >
    {() => html`
      <header>
        <pos-login />
      </header>
      <main>
        <pos-navigation
          uri="" />
      </main>
    `}
  </Story>
</Canvas>
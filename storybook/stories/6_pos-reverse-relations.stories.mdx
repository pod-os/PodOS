import {html} from "lit-html";

import {
  Canvas,
  Meta,
  Story
} from '@storybook/addon-docs/blocks';

<Meta
  title="Basics"
  args={{ uri: "https://solidproject.solidcommunity.net/profile/card#me" }}
  parameters={{
    actions: {
      handles: ['pod-os:link'],
    },
  }}
/>


## pos-reverse-relations

Renders a table containing links to resources that reference the current resource.

<Canvas
  withSource="open">
  <Story
    name="pos-reverse-relations"
  >
    {({uri}) => html`
<pos-resource uri=${uri}>
   <pos-reverse-relations />
</pos-resource>
    `}
  </Story>
</Canvas>
